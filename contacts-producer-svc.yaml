apiVersion: v1
kind: Service
metadata:
  name: contacts-producer-service
spec:
  type: NodePort
  ports:
    - port: 8081
      targetPort: 8080
      nodePort: 30080
  selector:
    app: contacts-producer

